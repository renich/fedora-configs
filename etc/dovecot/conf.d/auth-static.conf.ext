# Static passdb. Included from auth.conf.

passdb {
  driver = passwd-file
  args = scheme=SSHA512.B64 username_format=%u /etc/virtual/%d/passwd
}

userdb {
  driver = static
  args = uid=mail gid=mail scheme=SSHA512.B64 username_format=%u home=/var/spool/mail/virtual/%d/%n
}
